Layout
 Navbar (responsive)
 Footer
 Main layout wrapper

Pages
 Home (hero + intro)
 About
 Events (fetch /api/events)
 Gallery (static images or Instagram)
 Contact (form → /api/contact)

API
 services/api.js

getEvents()

sendContactForm()
 Use /api/... (no localhost)

Components
 EventCard
 Button
 Inputs

UI / UX
 Responsive (mobile-first)
 Clean spacing + typography
 Loading + error states

Accessibility
 Alt text images
 Proper headings
 Keyboard navigation
 Focus states

Final
 npm run build
 No console errors

 page contents:
 home: hero section, piccola introduzione ad ogni pagina.
 eventi: elenco delgi eventi, sia passati che arrivanti.
 galleria: foto, e solo foto, filtrabile per periodo.
 chi siamo: introduzione elaborata alla squadra e membri
 donazioni: pagina complicata che abilita i visitatori a donare all'associazione
 contatti: un elenco di contatti, con un form per gli mail